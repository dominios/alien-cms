var componentStructure = {
    meta: {
        // meta is same for each component
        type: 'Application\MyComponentClassName',
        name: 'My Component Name'
    },
    body: {
        // body can be anything
        // either values already set or as variables which are set by componentInstance
    }
};
