var versioning = {
    // VERSIONING - might be part of all documents
    // defines same structure for keeping versions data
    version: 1, // int: incremental number
    previousVersions: [
        "listOfIds ???"
    ]
};