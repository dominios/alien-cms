var users = {
    // not sure if add into DB or just JSON on the filesystem
    admin: {
        password: 'hash',
        email: 'admin@admin.sk',
        access: {
            // @todo
        }
    }
};
